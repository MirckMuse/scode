import { EditorStateConfig } from "@scode/state";

export interface EditorViewConfig {
  // TODO:
  parent:

}

export class EditorView {
  visible_ranges() {
  }

  constructor(config: EditorViewConfig) {
    config = config || {};
  }
}
